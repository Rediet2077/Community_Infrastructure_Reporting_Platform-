# ============================================================
# CIRP — Community Infrastructure Reporting Platform
# .gitignore — root (covers backend, frontend, mobile, ai)
# ============================================================
