# AI Duplicate Detection

This folder contains the duplicate report detection pipeline for CIRP.

- `dataset/` — data used to train and validate duplicate detection.
- `notebooks/` — experiments and model evaluation notebooks.
- `src/` — duplicate detection algorithms and utilities.
- `models/` — trained models and embeddings.
- `tests/` — unit tests for duplicate detection components.
