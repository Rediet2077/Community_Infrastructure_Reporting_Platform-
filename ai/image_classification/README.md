# AI Image Classification

This folder contains the image classification pipeline for CIRP.

- `dataset/` — raw and processed image data.
- `notebooks/` — exploratory analysis and training notebooks.
- `src/` — data preprocessing, model training, and inference code.
- `models/` — saved trained models.
- `tests/` — unit tests for image classification components.
